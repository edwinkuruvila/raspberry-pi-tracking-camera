[Unit]
Description=pigpio GPIO daemon for room-camera servos
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/pigpiod -g -n 127.0.0.1
Restart=on-failure
RestartSec=2

[Install]
WantedBy=multi-user.target
